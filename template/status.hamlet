<h3>Judge Status

<form action="@{StatusR}" method="get">
  Contest:<input type="text" size="10" name="contest" value="#{contestId}">
  User:<input type="text" size="10" name="name" value="#{user}">
  Type:
    <select name="type">
      <option value="Aizu"> Aizu
  Problem Sets:<input type="text" size="10" name="problem" value="#{pid}">
  Number:<input type="text" size="10" name="number" value="#{num}">
  <input type="submit" name="search" value="search">

  $if contestId /= ""
    <a href="@{ContestR $ TS.pack contestId}">Go to Contest Page
  $if (jtype /= "") && (pid /= "")
    <a href="#{getDescriptionURL (read jtype) pid}" target="blank">Go to Problem Page

<table summary="status table">
  <thead>
    <tr>
      <th>ID
      <th>CID
      <th>Datetime
      <th>User
      <th>Type
      <th>Problem
      <th>Judge
      <th>Time
      <th>Memory
      <th>Size
      <th>Lang
      <th>

  $forall (id, s) <- statusList
    <tbody>
      <tr class="#{cssClass (submitJudge s)}">
        <td class="id">
          <a href="@{SourceR id}">#{id}
        <td class="id">
          <a href="@{StatusR}?contest=#{submitContestnumber s}">#{submitContestnumber s}
        <td class="datetime">#{submitSubmitTime s}
        <td class="user">
          <a href="@{StatusR}?name=#{submitUserId s}">#{submitUserId s}
        <td class="problem">#{submitJudgeType s}
        <td class="problem">
          <a href="@{StatusR}?type=#{submitJudgeType s}&amp;problem=#{submitProblemId s}">#{submitProblemId s}
        <td class="judge">#{submitJudge s}
        $if (submitTime s) == "0"
          <td class="time">0
        $else
          <td class="time">#{submitTime s}0
        <td class="memory">#{submitMemory s}
        <td class="size">#{submitSize s}
        <td class="language">#{submitLang s}
        <td class="rejudge">
          <a href="@{RejudgeR id}">rejudge

